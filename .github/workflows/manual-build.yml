name: Manual Build with Custom Version

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v0.0.1, v1.2.3)'
        required: true
        default: 'v0.0.1'

env:
  REGISTRY: docker.io
  IMAGE_NAME: yongzz668/sqnb

jobs:
  manual-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Create version file
      run: |
        cat > version.json << EOF
        {
          "version": "${{ github.event.inputs.version }}",
          "build_time": "$(date -u +"%Y-%m-%d %H:%M:%S")",
          "commit_hash": "${{ github.sha }}"
        }
        EOF

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        platforms: linux/amd64,linux/arm64

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        platforms: linux/amd64,linux/arm64
        push: true
        tags: |
          ${{ env.IMAGE_NAME }}:${{ github.event.inputs.version }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Success message
      run: |
        echo "🎉 手动构建完成!"
        echo "版本: ${{ github.event.inputs.version }}"
        echo "镜像: ${{ env.IMAGE_NAME }}:${{ github.event.inputs.version }}"
